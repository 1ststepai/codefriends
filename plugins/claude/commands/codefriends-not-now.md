---
description: Snooze the CodeFriends connect prompt (ask again later)
---

Run this command with the Bash tool:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/connect.mjs" --provider claude --action not-now
```

Confirm that CodeFriends will ask again after the cooldown (a few days).
