---
description: Never ask to connect CodeFriends again in Claude Code
---

Run this command with the Bash tool:

```bash
node "${CLAUDE_PLUGIN_ROOT}/scripts/connect.mjs" --provider claude --action dont-ask
```

Confirm that the connect prompt is stored as “Don’t ask again” for Claude Code only.
