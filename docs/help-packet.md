# Help packet

A **help packet** is a short markdown brief you write on purpose so a friend can finish stuck work **on their own AI usage** (Cursor, Claude, Codex, Gemini, or similar), then send back a PR or patch.

You create the packet to share. They accept voluntarily. They run agents on **their** account — this does not stretch anyone else's vendor quota. Contents are only what you type; CodeFriends does not scrape chat history. Copy or download the `.md` file and paste it into a provider chat. Nothing is auto-posted to socials.

Use the **Help packet** creator on the CodeFriends Library tab, or copy the template below. Path-specific templates (School Paths 01–08): [help-packets/](./help-packets/).

```markdown
# Help packet: (title)

This file is a **help packet**. The author is asking a friend to finish stuck work **on the friend's own AI usage** (Cursor, Claude, Codex, Gemini, or similar). Accepting is voluntary. Run agents on your own account — this packet does not use anyone else's vendor quota.

Only the notes below are included. This is not a dump of chat history.

When you are done, send back a PR (preferred) or a patch. Do not post this packet to socials unless the author asks you to.

## 1. Goal

What done looks like.

## 2. Context

- Repo URL:
- Branch:
- Relevant paths:

## 3. Constraints

Don't touch X. Stack notes.

## 4. What's blocked / tried

## 5. Success criteria

## 6. How to send back

Open a pull request (preferred). A patch file is fine if a PR is not practical.

## 7. Optional: CodeFriends library item
```
